import styled from "styled-components";
import { Palette } from "./assets/palette/Palette";

const ContainerApp = styled.div``;

const ContentApp = styled.div`
  background-color: ${Palette.BrandGray100};
`;

export const StyledApp = {
  ContainerApp,
  ContentApp,
};
